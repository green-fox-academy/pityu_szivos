import java.util.ArrayList;
import java.util.Arrays;
/*import java.util.Collections;
import java.util.Scanner;

// MOT READY

public class arraylist_calculator {
    public static void main(String[] args) {


        Scanner scanner = new Scanner(System.in);
        System.out.println("Hello, user! Choose one of the following expression: -, +, *, / or %: ");
       String char_input = scanner.nextLine();

      Scanner scanner_2 = new Scanner(System.in);
        System.out.println("Give me your first number: ");
       int number_input= scanner_2.nextInt();

       Scanner scanner_3 = new Scanner(System.in);
        System.out.println("Give me your second number:");
       int number_input_2= scanner_3.nextInt();


       char expression = char_input.charAt(0);
       char num_1 = (char) number_input;
       char num_2 =(char) number_input_2;
       //char num_1 = '5';
        c//har num_2 = '2';


        calculator(expression, num_1, num_2);
    }
    public static char calculator(char do_expression, char num_a, char num_b){

        int x = Character.getNumericValue(num_a);
        int y = Character.getNumericValue(num_b);


        if(do_expression == '+'){

            System.out.println(x + y);

        }
        else if(do_expression == '-'){

            System.out.println(x -y);
        }
        else if( do_expression == '*'){

            System.out.println(x *y);
        }
        else if(do_expression == '/'){
            System.out.println(x / y);
        }
        else{
            System.out.println(x % y);
        }
     return do_expression;
    }


    // Create a simple calculator application which reads the parameters from the prompt
    // and prints the result to the prompt.
    // It should support the following operations,
    // create a method named "calculate()":
    // +, -, *, /, % and it should support two operands.
    // The format of the expressions must be: {operation} {operand} {operand}.
    // Examples: "+ 3 3" (the result will be 6) or "* 4 4" (the result will be 16)

    // You can use the Scanner class
    // It should work like this:

    // Start the program
    // It prints: "Please type in the expression:"
    // Waits for the user input
    // Print the result to the prompt
    // Exit

} */


