public class notes_hash_tables {
    public static void main(String[] args) {


        // speedy insertion, deletion is necessary
        // hash table an array with a # function
        // with # function you can search later -> all elements have # values
       // # store only first letter of key -> easier to look for
        // can contain all data types

        // collision -> two keys hashing to the same index -> get sent to another place -> clustering
        // separate chaining -> more than one array ->
    }
}
